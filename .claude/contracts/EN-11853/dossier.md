# Dossier — EN-11853

- **Ticket:** EN-11853
- **Validated at:** `146d3b358fd1d80f40ace397ee9b18b5ec0b00d2`
- **Base commit:** `4279bc29262b73b65027302dbeff5978274a9cdc`
- **Seal:** verified — clauses unchanged since Gate 2
- **Signature:** recorded in signature.json, not here — this document is
>   the thing being signed, so it cannot carry its own signature

> Generated by `build_dossier.py`. Every verdict below came from a recorded
> command execution; nothing here is written by an agent.

## Needs your attention

Nothing. All clauses passed with frozen paths intact.

## Passed

| Clause | Tier | Kind | Command | Exit |
|---|---|---|---|---|
| C1 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b1_probes_test.yaml'` | 0 |
| C2 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b2_init_containers_test.yaml'` | 0 |
| C3 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b3_extra_volumes_test.yaml'` | 0 |
| C4 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b4_lifecycle_test.yaml'` | 0 |
| C5 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b5_envfrom_test.yaml'` | 0 |
| C6 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b6_termination_grace_period_test.yaml'` | 0 |
| C7 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b7_dnsconfig_test.yaml'` | 0 |
| C8 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b8_hostaliases_test.yaml'` | 0 |
| C9 | behavior | additive | `helm unittest gremlin -f 'tests/en11853_b9_podsecuritycontext_test.yaml'` | 0 |
| C10 | property | invariant | `diff <(helm template my-release gremlin -f gremlin/tests/values-secretauth-test.yaml -s templates/daemonset.yaml) gremlin/tests/fixtures/en11853_backward_compat.gremlin.yaml && diff <(helm template my-release gremlin -f gremlin/tests/values-secretauth-test.yaml -s templates/chao-deployment.yaml) gremlin/tests/fixtures/en11853_backward_compat.chao.yaml` | 0 |
| C11 | property | invariant | `helm unittest gremlin -f 'tests/en11853_extraenv_exempt_test.yaml'` | 0 |

## Signature


Signing is the ship decision. Verify the seal yourself at any time:

```bash
sha256sum .claude/contracts/EN-11853/clauses.json
```
